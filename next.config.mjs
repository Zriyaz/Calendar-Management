/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', "lh3.googleusercontent.com", "utfs.io"], // Add other domains if needed
      },
};

export default nextConfig;
